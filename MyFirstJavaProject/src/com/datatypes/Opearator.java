package com.datatypes;

/**
  operators-
  1.Arithmetic : +,-,/,*,%,++,--
  2.Relational : >,<.>=,<=,!=,==
  3.Logical : &&,||,!
  4.Bitwise : &,|,~,^
  5.shift :
          << (left shift),
          >> (right shift with sign bit fill),
          >>> (right shift with zero fill)
  6.Conditional:
          ? :(ternary operator)
   **/               
public class Opearator {

	public static void main(String[] args) {
		int obtain=290, total=300;
		double per;
		per=(double)obtain/total*100;
		System.out.println("Persentage ="+per);
		
		
	}

}
